{
  "name": "task-management-app",
  "version": "1.0.0",
  "description": "Full-stack Task Management Application",
  "scripts": {
    "build": "npm install --prefix client && cd client && npm run build",
    "start": "cd client && npm start"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {},
  "dependencies": {}
}